# Blocked

Tasks that want to proceed but cannot until something outside them changes: a
decision, an approval, an Architecture Decision Record, or another task. The latest
`Log` entry in each file names the blocker and who can clear it.

This file is not a task; it keeps the folder in Git when nothing is blocked. See
`../README.md`.
